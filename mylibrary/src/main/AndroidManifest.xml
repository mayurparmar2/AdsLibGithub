<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application>
        <!-- AD_SERVICES_CONFIG conflict: play-services-ads and Firebase
             measurement both declare this property. Force the GMA (ads) config. -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />
    </application>
    <!--
        NOTE: Do NOT declare com.google.android.gms.ads.AD_MANAGER_APP here.
        This library uses AdMob (com.google.android.gms.ads.*) APIs, not Google
        Ad Manager. Declaring AD_MANAGER_APP=true would merge into every consuming
        app and force the GMA SDK into Ad Manager mode, breaking AdMob init.
        The consuming app must declare com.google.android.gms.ads.APPLICATION_ID.
    -->
</manifest>